# Requirements for breast_cancer_cnn.ipynb
# Note: if running in Google Colab, tensorflow, numpy, pandas, matplotlib, seaborn,
# and scikit-learn are pre-installed -- only kagglehub needs installing (handled by
# the notebook's own `!pip install -q kagglehub` cell). This file matters most for
# running the notebook locally (Jupyter, VS Code, etc.) outside Colab.

tensorflow>=2.15
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
kagglehub>=0.3
